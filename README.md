Script increments volumes of instance specified by it's Name tag value. Increment size is specified inside the script as well.

Depending on flags, script can either resize only root instance, or all volumes related to to specified by Name tag instance.

To get more in formation on how exactly to use script, run it with -h flag to get Help propt.